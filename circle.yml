machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t gentoo ./:
        timeout: 3600
    - docker run -d gentoo
    - ./ci-slim.sh
    - docker images -a --digests
    - docker history gentoo
    - docker history gentoo.slim
