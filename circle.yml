machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker:
      - timeout: 3600

test:
  override:
    - docker build -t gentoo:ci .
    - docker run -d gentoo:ci
    - ./ci-slim.sh
