machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t gentoo:ci .:
      - timeout: 3600

test:
  override:
    - docker run -d gentoo:ci
    - ./ci-slim.sh
