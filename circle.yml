machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t gentoo:ci ./:
        timeout: 3600
    - docker run -d gentoo:ci
    - ./ci-slim.sh
    - docker images -a --digests
    - docker history gentoo:ci
    - docker history gentoo:ci.slim
